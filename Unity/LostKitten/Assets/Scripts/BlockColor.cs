using UnityEngine;
using System.Collections;

public enum BlockColor  // enum aanmaken van alle kleuren van het kleurenspectrum. 
{   
  Red, Orange, Yellow, Green, Blue, Purple 
}

// we gebruiken een enum en geen array, omdat je bij een array je een bepaald datatype telkens moet opgeven, en hier maak je zelf een datattype(blockcolor)